apiVersion: v1
kind: Service
metadata:
  name: rms-catalog-service
  namespace: rms-catalog
spec:
  selector:
    app: rms-catalog
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3002